# Local dev only (gitignored). The committed fallback is frontend/src/deployed.ts.
# After `node deploy.mjs`, VITE_CONTRACT_ADDRESS is filled in automatically.
VITE_CONTRACT_ADDRESS=0xYourDeployedContractAddressHere
PRIVATE_KEY=your_private_key_here
